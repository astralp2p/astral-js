// api/apphost/consts — apphost op strings for the apphost protocol, one source of truth.

/** The `apphost.*` operation names, sent as the op of an apphost query. */
export const Ops = {
  register: 'apphost.register',
} as const;
